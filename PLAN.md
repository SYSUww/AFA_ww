# AFAC2026 赛题四实施方案 v2

## 目标

先完成 `regulatory` 领域 A 榜高准确率基线，同时把工程组织成“共享骨架 + 领域插件”，为 `financial_reports` 等其他领域接入留出接口。

## 首版范围

- 共享运行骨架
- `.env` 模型配置读取
- 文档清单构建
- 法规域解析、切分、索引、检索、作答
- `answer.csv` 与 `evidence.json` 导出
- 运行日志、token 统计、误差分析

## 首版原则

- 不使用向量检索
- A 榜直接使用题目给定 `doc_ids`
- 证据单元可追溯
- 题型流程可分开实现，但统一输出结构
- 所有模型调用都记录 token

## 当前默认

- 主模型从 `.env` 读取，允许兼容 OpenAI 风格接口
- 法规主文档优先使用 `txt/html`
- 附件 PDF 使用 `pypdf` 兜底抽文本
- 第二样板域预留为 `financial_reports`

## 当前项目优化任务清单

### P0

- 提升 PDF 解析质量，补一层更强兜底解析，优先覆盖财报、保险、合同里表格和公式密集文档
- 为解析结果保留更多结构信息，包括页码、标题、段落层级、表格痕迹和字段来源
- 优化财报结构化抽取，把指标名、年份、单位、数值拆字段，重点覆盖营收、归母净利润、经营现金流、研发投入、研发占比、分红
- 优化保险结构化抽取，拆出触发条件、给付规则、公式片段、现金价值、账户价值、已交保费、基本保额
- 做题型化检索改写，从题干和选项抽实体词、年份词、金额词、指标词，按题型生成 query
- 做多文档平衡召回，跨文档题保证每个 `doc_id` 都有候选证据，避免单文档淹没
- 给不同 `EvidenceUnit` 加权，让 `metric_row`、`formula_block`、`element_block` 高于普通段落
- 为财报比较题加本地规则判定，对同比、分红、研发占比、现金流题优先本地算，再让模型解释
- 为保险计算题加本地计算骨架，先抽候选数值和公式，再让模型判断适用条件
- 按题型拆 prompt，为单选、多选、真假、计算题分别设计输出模板
- 压缩送给模型的上下文，优先保留高价值证据，降低 token 消耗
- 增强答案合法性校验，拦截空答案、非法多选、非法真假输出，并自动修正

### P1

- 优化合同要素抽取，覆盖发行主体、规模、期限、评级、票面利率、承销机构、受托管理人、回售和赎回条款字段
- 优化研报切分，区分数据句、结论句、预测句、趋势判断句
- 做数值和单位标准化，统一亿元、万元、百分比、bps、中文数字等表达
- 增加规则重排，在字段命中、数值命中、标题命中、年份命中时加分
- 做题目标签体系，自动区分比较题、计算题、条款题、真假题、跨文档题
- 增强证据链导出，每题保存 query、topk、重排后分数和最终采用证据
- 增加错因分类，区分解析错、检索错、抽取错、推理错、格式错
- 统一 run 元数据，明确每个 run 的模型、prompt 版本、索引版本、生成时间
- 增强高置信规则覆盖，当规则结果明显可靠时约束或覆盖模型异常答案

### P2

- 扩展 manifest 元数据，增加页数、文档类型、解析来源、解析时间、异常标记
- 优化监管领域切分粒度，把章节、条、款进一步拆细
- 增加答案冲突告警，在证据支持与模型结论明显冲突时单独标记
- 生成领域级误差报告，统计每个领域最常见的失败模式
- 做 run 和 retrieval 的可视化整理，让目录更容易看出结果来源和生成方式
- 预留 B 榜接口，先不做盲检索，但把候选文档选择器接口留好

## 建议执行顺序

1. 先完成 P0 里的解析、结构化抽取、检索改写
2. 再完成 P0 里的财报和保险规则层
3. 然后处理 P0 里的 prompt 压缩和答案校验
4. 再推进 P1 的合同、研报、重排与错因分析
5. 最后补齐 P2 的治理和可视化
